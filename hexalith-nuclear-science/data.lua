require("prototypes.achevement")
require("prototypes.surfaces")

if mods["any-planet-start"] then
  require("compatibility.any-planet-start.data")
end

if mods["carbon"] then
  require("compatibility.carbon.data")
end